---
presence_manager:
  module: presence
  class: PresenceManager
  dependencies:
    - aaron
    - britt
  entity_ids:
    edge_threshold: input_select.edge_radius
    home_threshold: input_select.home_radius
    nearby_threshold: input_select.nearby_radius
    proximity_zone_sensor: sensor.proximity_zone
